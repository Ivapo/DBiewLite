{
  "name": "dbiewlite-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "mkdir -p src/public && cp node_modules/sql.js/dist/sql-wasm.wasm src/public/",
    "predev": "mkdir -p src/public && cp node_modules/sql.js/dist/sql-wasm.wasm src/public/",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.3.0"
  }
}
