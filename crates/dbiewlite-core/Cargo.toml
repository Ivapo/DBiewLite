[package]
name = "dbiewlite-core"
version = "0.1.0"
description = "Shared SQLite operations for DBiewLite"
edition = "2021"
license = "MIT"

[dependencies]
rusqlite = { version = "0.34", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
